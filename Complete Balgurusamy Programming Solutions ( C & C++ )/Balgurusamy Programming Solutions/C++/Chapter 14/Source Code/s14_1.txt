#include<iostream>
#include<vector>
#include<conio.h>
using namespace std;

void output(vector<int> &);

int main(void)
{
	int i;
	cout<<"\n\nCreating An Integer Vector of Size 10...";
	vector<int>v1(10);
	cout<<"\n\nSetting The First Element of Vetor to 0...";
	v1[0]=0;
	cout<<"\n\nSetting The Last Element of Vetor to 9...";
	v1[9]=10;
	cout<<"\n\nSetting The Others Element of Vetor to 1...";
		for(i=1;i<=8;i++)
			v1[i]=1;
	cout<<"\n\nDisplaying The Contents of Vector Elements...";
	output(v1);
	cout<<"\n\nPress Any Key To Continue....";
	getch();
	return(0);
}

void output(vector<int> &v)
{
	int i;
		for(i=0 ; i<v.size() ; i++)
			cout<<"\n\nElement "<<(i+1)<<" :  "<<v[i];
}
